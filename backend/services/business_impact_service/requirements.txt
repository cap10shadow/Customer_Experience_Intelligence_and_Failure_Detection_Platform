fastapi==0.109.2
uvicorn==0.27.1
# Phase 13 Batch 10: minimal, explicitly-authorized dependency fix for a
# pre-existing gap discovered in Batch 9 -- this service's own
# app/services/business_impact_event_publisher.py (and main.py) import
# httpx directly, but it was never listed here (only the
# opentelemetry-instrumentation-httpx *instrumentation* package was,
# which does not itself depend on httpx). Crashed on every fresh build,
# dev or production-like, with `ModuleNotFoundError: No module named
# 'httpx'`. Version matches every sibling service that already declares
# it (gateway_service, copilot_service).
httpx==0.27.0
pydantic-settings==2.1.0
sqlalchemy==2.0.36
asyncpg==0.29.0
prometheus-client==0.24.1
opentelemetry-api==1.44.0
opentelemetry-sdk==1.44.0
opentelemetry-exporter-otlp-proto-grpc==1.44.0
opentelemetry-instrumentation-fastapi==0.65b0
opentelemetry-instrumentation-httpx==0.65b0
opentelemetry-instrumentation-sqlalchemy==0.65b0
